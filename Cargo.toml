[package]
name = "stateful"
version = "0.1.0"
edition = "2018"
license = "MIT"
description = "Hierarchial state machine with function pointers"
readme = "README.md"
keywords = ["hierarchial state machine"]


[dependencies]
stateful_macro = { path = "macro" }

[dev-dependencies]
heapless = "0.7"
unit-enum = { git = "https://github.com/mdeloof/unit-enum.git" }

[features]
